{
  "name": "Emojifox",
  "description": "Emojipanel in browser sidebar.",
  "version": "0.1.0",
  "private": true,
  "license": "UNLICENSED",
  "scripts": {
    "start:firefox": "web-ext run",
    "build": "web-ext build --artifacts-dir=dist",
    "lint": "web-ext lint",
    "sign": "web-ext sign"
  },
  "devDependencies": {
    "web-ext": "^2.4.0"
  }
}
